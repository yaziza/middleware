CREATE TABLE ORDER (ID INTEGER NOT NULL, PRIMARY KEY (ID))
CREATE TABLE CUSTOMER (ID INTEGER GENERATED BY DEFAULT AS IDENTITY NOT NULL, NAME VARCHAR(255), PRIMARY KEY (ID))
CREATE TABLE PRODUCT (ID INTEGER NOT NULL, DESCRIPTION VARCHAR(255), PRIMARY KEY (ID))
CREATE TABLE PRODUCTINSTANCE (ID INTEGER NOT NULL, AMOUNT INTEGER, PRODUCT_ID INTEGER, PRIMARY KEY (ID))
CREATE TABLE ORDER_PRODUCTINSTANCE (Order_ID INTEGER NOT NULL, productInstances_ID INTEGER NOT NULL, PRIMARY KEY (Order_ID, productInstances_ID))
CREATE TABLE CUSTOMER_ORDER (Customer_ID INTEGER NOT NULL, orders_ID INTEGER NOT NULL, PRIMARY KEY (Customer_ID, orders_ID))
ALTER TABLE PRODUCTINSTANCE ADD CONSTRAINT PRDCTNSTANCEPRDCTD FOREIGN KEY (PRODUCT_ID) REFERENCES PRODUCT (ID)
ALTER TABLE ORDER_PRODUCTINSTANCE ADD CONSTRAINT RDRPRDprdctnstncsD FOREIGN KEY (productInstances_ID) REFERENCES PRODUCTINSTANCE (ID)
ALTER TABLE ORDER_PRODUCTINSTANCE ADD CONSTRAINT RDRPRDCTNSTNCErdrD FOREIGN KEY (Order_ID) REFERENCES ORDER (ID)
ALTER TABLE CUSTOMER_ORDER ADD CONSTRAINT CSTOMERORDERrdrsID FOREIGN KEY (orders_ID) REFERENCES ORDER (ID)
ALTER TABLE CUSTOMER_ORDER ADD CONSTRAINT CSTMRORDERCstmerID FOREIGN KEY (Customer_ID) REFERENCES CUSTOMER (ID)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT DECIMAL(15), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 0)
